mixin page_header(title = 'Chưa nhập tiêu đề')
    div(class="flex items-center justify-between py-[1rem]") 
        h1(class="w-full text-[20px] font-bold text-[#192335]") #{title}
        div(class="flex gap-x-[1rem]")
            block